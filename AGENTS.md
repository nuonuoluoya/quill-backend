# 后端协作规则

- GitHub 仓库为 https://github.com/nuonuoluoya/quill-backend；远端名称为 origin，首次关联时检查并保留已有远端历史。

- 规格唯一维护源为 `D:\agent\SPEC\Quill\SPEC.md`。每次后端修改必须先更新规格中的相关行为、接口、约束或验收标准，再修改代码；缺陷修复也先明确预期行为与回归验收要求。
- 本仓库 `docs/SPEC.md` 是上述规格的同步快照，不独立编辑。提交前从唯一维护源同步，确保规格和代码进入同一次相关提交。
- 完成实现和相关检查后，提交本次相关变更并推送到用户指定的 GitHub 远端；报告提交、检查与推送结果。未配置远端或推送失败时明确说明，不宣称已经上传。

- 特性、用户可见行为、配置、启动方式或接口发生变化时，在同一次提交中更新 README.md。
- 提交前检查分支、远端及工作区，只提交与任务相关的变更，并运行相关检查。
- 不提交 .env、.data、实际 AppID、密钥、数据库或私人音频；新增配置同步更新 .env.example。
- 不覆盖本地配置，不强制推送或重写历史，除非用户明确要求。
- 保持项目独立运行，文件路径以项目根目录为基准。
